name: simulador_credito_rispar
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  mobx: ^2.0.7+3
  flutter_mobx: ^2.0.6+1
  mask_text_input_formatter: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mobx_codegen: ^2.0.7
  build_runner: ^2.1.11


flutter:
  uses-material-design: true

  assets:
    - assets/images/
